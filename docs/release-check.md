# Проверка перед отправкой в GitHub

Дата: 2026-09-21. Исходный коммит: `c73a942`.

## Выполнено

- Production-сборка React после чистого `npm ci` — успешно.
- Все 183 backend-теста на отдельной временной PostgreSQL — успешно.
- Все миграции применились к пустой тестовой БД.
- `makemigrations --check --dry-run` — изменений моделей без миграций нет.
- Django production-check — без ошибок; остаются рекомендации
  `security.W005` и `security.W021` о HSTS для поддоменов и preload.
  Эти опции не включались автоматически.
- Сборка статики WhiteNoise — успешно.
- В production-настройках проверены HTTP `/health/`, HTTPS `/login/`,
  выдача JS/CSS React и перенаправление остальных HTTP-запросов на HTTPS.
- Проверены синтаксис shell-скриптов, LF, TOML и `git diff --check`.
- В отслеживаемых исходниках после исключения бэкапа не найдены явные
  приватные ключи, токены GitHub/Telegram и URL с паролем по проверенным шаблонам.
  Это ограниченная проверка, а не полный аудит безопасности.

## Исправлено

- Миграции, таблица кеша и первоначальный администратор создаются только в pre-deploy.
  Перезапуск web собирает статику и запускает Gunicorn.
- Railway проверяет `/health/` перед переключением трафика.
- Исправлена ошибка PostgreSQL при назначении мастера заказу без города:
  блокируется запись мастера, а не необязательные строки связей.
- Экспорт расчётов при отсутствующей ставке возвращает понятную ошибку 400,
  включая расчёт входящего остатка, вместо ошибки сервера или неверного итога.
- Тесты адаптированы к текущей модели ставок `WorkerServiceRate`,
  обязательному городу, маршрутам и интерфейсу. Расчётные суммы проверяются.
- Подсказка профиля соответствует текущей логике: используется ставка по услуге.
- Добавлены `.gitattributes`, шаблон переменных и GitHub Actions
  для сборки, PostgreSQL-тестов, production-check и Docker build.
- README сохранён в UTF-8 и содержит инструкции запуска.

## Перед публичной публикацией

Файл `backups/before_cleanup_20260913_221726.json` исключён из индекса Git;
локальная копия сохранена и теперь игнорируется. В нём обнаружены записи
пользователей, клиентов, заказов, Telegram и сессии.

Файл остаётся в старых коммитах, включая исходную историю проекта.
Новый коммит с удалением файла не удаляет старые версии.
Перед публичной отправкой нужна согласованная очистка истории либо новый
репозиторий из проверенных исходников без старой истории. Перезапись истории
и force-push в рамках этой подготовки не выполнялись.

## Ограничения проверки

Docker на этом компьютере отсутствует, поэтому локально образ не запускался.
Проверка Docker build добавлена в GitHub Actions; её результат будет известен
после отправки коммита. Браузерные Playwright-тесты в эту проверку не входили.
Реальные переменные, PostgreSQL и HTTPS второго сервера Railway нужно проверить
после деплоя по [инструкции](railway.md).

Рабочая база CRM не мигрировалась и не менялась этой проверкой.
Отправка в GitHub и деплой не выполнялись.
