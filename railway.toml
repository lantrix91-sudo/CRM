[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = "python backend/manage.py migrate --noinput && python backend/manage.py createcachetable"
startCommand = "sh /app/deploy/start-web.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
